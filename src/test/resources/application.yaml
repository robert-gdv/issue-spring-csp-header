spring:
  cloud:
    gateway:
      routes:
        - id: webserver
          uri: ${webserver.url}
          predicates:
            - Path=/**
            - Method=GET
          filters:
            - SecureHeaders
